version: '3.8'

services:
  tooncar-racing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tooncar-racing-3d
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
